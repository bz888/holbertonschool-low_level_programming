#include "main.h"
#include <stdio.h>

/**
 * mul - check the code
 * @a: character to check (as int)*
 * @b: character to check (as int)
 * Return: Always 0.
 */
int mul(int a, int b)
{
	return (a * b);
}
